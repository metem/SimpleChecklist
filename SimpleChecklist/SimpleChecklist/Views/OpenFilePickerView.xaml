<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SimpleChecklist.Views.OpenFilePickerView">

    <ListView x:Name="ParentContext" ItemsSource="{Binding FilesList}" VerticalOptions="FillAndExpand"
              HorizontalOptions="FillAndExpand">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ViewCell.View>
                        <Label
                            Text="{Binding Value}"
                            FontSize="20"
                            HorizontalOptions="Start"
                            VerticalOptions="CenterAndExpand">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Path=BindingContext.FileChoosenCommand, Source={x:Reference ParentContext}}"
                                    CommandParameter="{Binding}" />
                            </Label.GestureRecognizers>
                        </Label>
                    </ViewCell.View>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>